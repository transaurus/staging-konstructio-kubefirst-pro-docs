apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubefirst-pro-docs.fullname" . }}-test-connection"
  labels:
{{ include "kubefirst-pro-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kubefirst-pro-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
